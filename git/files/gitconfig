# Notice that:
#
#  1. A semicolon starts a comment.
#  2. To use semicolons in any entry, enclose it in double quotes.
#

[include]
        path = ~/.gitconfig_generic
        path = ~/.gitconfig_platform_specific
        path = ~/.gitconfig_credentials
